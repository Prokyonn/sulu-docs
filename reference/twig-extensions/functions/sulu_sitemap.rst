``sulu_sitemap``
================

Returns the sitemap for a given webspace and locale (defaults to the current locale and webspace).

**Arguments**:

- **locale**: *string* - The locale for which to determine the sitemap (**optional**).
- **webspaceKey**: *string* - The webspace for which to determine the sitemap (**optional**).

